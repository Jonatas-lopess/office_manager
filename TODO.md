# Setup Drizzle & cr-sqlite

- [x] Create `drizzle.config.ts` for drizzle-kit
- [x] Add `db:generate` and `db:studio` scripts to `package.json`
- [x] Create a local migration runner/script for cr-sqlite
- [x] Create a React Context Provider for the DB instance
- [x] Update `main.tsx` to initialize and provide the DB
- [x] Test DB read/write operations using the `clients` table
